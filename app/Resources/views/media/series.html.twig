{% extends "templates/layout.html.twig" %}
{% block content %}
    <div class="container">
        <h2>
            <span class="underline">Séries</span>
            <select class="selectpicker" data-style="btn-danger">
                <option>Catégorie</option>
            </select>

            <select class="selectpicker" data-style="btn-danger">
                <option>Trier</option>
            </select>
        </h2>
        <div class="list">
            <ul class="flex-container">
                {% for serie in series %}
                    <li class="flex-item">
                        <div>
                            <a href="{{ path('serie-view', {'id': serie.id}) }}" >
                                <div class="cover" style='background: url("{{ asset('uploads/brochures/' ~ serie.brochure) }}");background-size: cover;'> </div>
                            </a>
                            <p><a href="{{ path('serie-view', {'id': serie.id}) }}" >{{ serie.title }}</a></p>
                        </div>
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}